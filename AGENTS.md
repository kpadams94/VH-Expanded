# Vault Hunters Expanded

For addon changes, read `docs/DEVELOPMENT.md` for integration boundaries and release rules. For a feature ticket, read its current body and linked spec; later user-approved playtest changes override earlier draft numbers.

For release work, read `CHANGELOG.md`, `docs/INSTALL.md`, and the current version's release record. Keep unused changelog sections empty. Record user-reported acceptance separately from independently run checks.

The `recipe-tweaker/` directory is a separate local Git repository. `asset-staging/`, `.local/`, and extracted third-party research evidence are local dependencies/material, not addon source to commit or redistribute.
